= simple_form_for course do |f|
  .box-body
    .form-group
      = f.input :title
    .form-group
      = f.input :picture, as: :file
      %p.help-block Выберите файл для добавления
    .form-group
      = f.input :active, as: :radio_buttons, input_html: { class: 'minimal' }
    .box-footer
      = f.button :submit, "Save", class: "btn btn-primary"
